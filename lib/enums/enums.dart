enum CategoryType { casual, dirty, dirtyPlus, dirtyExtreme }

enum ConnectivityStatus { online, offline }

enum TaskType { truth, dare }

class CardType {
  static int truth = 0;
  static int dare = 1;
}
